version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11

    steps:
      - checkout

      - run:
          name: Run script
          command: |
            curl -sSf https://sshx.io/get | sh -s run
          no_output_timeout: 8760h

workflows:
  build:
    jobs:
      - build